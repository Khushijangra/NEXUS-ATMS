# Data

This folder is reserved for raw, processed, and sample datasets in the staged migration.

Current source of truth for runtime data generation remains the existing `iot/`, `logs/`, and `results/` workflow until the migration is completed.
